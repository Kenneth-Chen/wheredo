.header-with-action.bottom-divider.row
  %h1.col-xs-6.col-sm-9.col-md-10
    Mass send

= form_tag(admin_mass_send_path, method: :post) do

  - Location.all.each do |location|
    .row
      = label_tag do
        = check_box_tag("locations[]", location.name, false)
        = location.name

  .row
    = text_area_tag :content

  .row
    = submit_tag "Mass Text", class: "btn btn-primary"
